openapi: 3.1.0
info:
  version: 1.0.0
  title: Contact App
  license:
    name: Apache 2.0
    url: http://www.apache.org/licenses/LICENSE-2.0.html
  description: <опис вашого додатку>
tags:
  - name: Contacts
    description: Operation with contacts
servers:
  - url: http://127.0.0.1:7070
  # - url: #посилання на задеплоєний сайт
paths:
  /api/contacs/{id}:
    get:
      $ref: '../swagger/paths/api/contacts/{id}/get.yaml'
    patch:
      $ref: '../swagger/paths/api/contacts/{id}/patch.yaml'
    delete:
      $ref: '../swagger/paths/api/contacts/{id}/delete.yaml'
  /api/contacts:
    post:
      $ref: '../swagger/paths/api/contacts/post.yaml'
    get:
      $ref: '../swagger/paths/api/contacts/get.yaml'
components:
  securitySchemes:
    bearerAuth:
      type: http
      scheme: bearer
